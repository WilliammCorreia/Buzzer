{% extends 'base.html.twig' %}

{% block title %}Buzzer · Accueil{% endblock %}

{% block body %}
    <div class="p-5 mb-4 bg-body rounded-3 shadow-sm">
        <div class="container-fluid py-3">
            <h1 class="display-5 fw-bold">🏀 Buzzer</h1>
            <p class="col-md-9 fs-5 text-secondary">
                Pronostiquez les matchs NBA, défiez vos amis au sein de ligues privées
                et grimpez dans les classements. Sans argent réel — juste pour la gloire.
            </p>
            {% if not app.user %}
                <a class="btn btn-primary btn-lg" href="{{ path('app_login') }}">Se connecter</a>
            {% else %}
                <span class="badge text-bg-success fs-6">Connecté en tant que {{ app.user.username }}</span>
            {% endif %}
        </div>
    </div>

    <h2 class="h4 mb-3">Prochains matchs</h2>
    {% if upcomingGames is empty %}
        <div class="alert alert-info">
            Aucun match programmé pour le moment. Lancez la synchronisation NBA ou chargez les fixtures.
        </div>
    {% else %}
        <div class="row row-cols-1 row-cols-md-2 g-3">
            {% for game in upcomingGames %}
                <div class="col">
                    <div class="card h-100 shadow-sm position-relative">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-center">
                                <span class="fw-semibold">{{ game.awayTeam.name }}</span>
                                <span class="text-secondary small">@</span>
                                <span class="fw-semibold">{{ game.homeTeam.name }}</span>
                            </div>
                            <div class="text-secondary small mt-2">
                                {{ game.startsAt|date('d/m/Y H:i') }}
                                · <span class="badge text-bg-{{ game.status|status_class }}">{{ game.status.label }}</span>
                            </div>
                            <a href="{{ path('app_game_show', {id: game.id}) }}" class="stretched-link" aria-label="Voir le match"></a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
{% endblock %}
