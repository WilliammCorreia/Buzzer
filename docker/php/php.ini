; Application PHP settings (loaded last, overrides defaults)
date.timezone = Europe/Paris
memory_limit = 256M

; Sensible dev error reporting; production should set display_errors = Off
display_errors = On
error_reporting = E_ALL

; OPcache — kept conservative for dev (validate timestamps so edits are picked up)
opcache.enable = 1
opcache.enable_cli = 0
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0

; Uploads / form posts
post_max_size = 16M
upload_max_filesize = 16M

realpath_cache_ttl = 600
